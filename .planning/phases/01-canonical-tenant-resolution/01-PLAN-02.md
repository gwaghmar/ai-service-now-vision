---
phase: 01-canonical-tenant-resolution
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/app/api/slack/slash/route.ts
  - src/app/api/v1/ingest/chat/route.ts
  - src/app/actions/admin.ts
  - src/app/(dashboard)/admin/integrations/page.tsx
  - src/app/(dashboard)/admin/integrations/integrations-form.tsx
autonomous: true
requirements:
  - TNTY-01
  - TNTY-02
  - TNTY-04
must_haves:
  truths:
    - "Slack slash resolves org via team_id before any request_type or catalog query."
    - "Chat ingest uses canonical resolver for verified organization id."
  artifacts:
    - path: src/app/api/slack/slash/route.ts
      contains: resolveOrgForSlackTeamId
    - path: src/app/api/v1/ingest/chat/route.ts
      contains: resolveChatIngestOrgId
  key_links:
    - from: src/app/api/slack/slash/route.ts
      to: src/server/tenant-resolution.ts
      pattern: resolveOrgForSlackTeamId
---

# Plan 02 — Wire Slack, chat ingest, admin Slack Team ID

## Objective

Call the canonical resolver before any org-scoped reads in Slack slash; route chat ingest through `resolveChatIngestOrgId`; let admins set `slack_team_id` for their org.

## Context

- @.planning/phases/01-canonical-tenant-resolution/01-RESEARCH.md
- @src/server/tenant-resolution.ts
- @src/app/api/slack/slash/route.ts
- @src/app/api/v1/ingest/chat/route.ts

<threat_model>
## Trust boundaries

| Boundary | Risk |
|----------|------|
| Slack payload → DB | Cross-tenant catalog read if org resolves wrong. |

## STRIDE (summary)

| ID | Category | Mitigation |
|----|----------|------------|
| T-2-01 | Information disclosure | Resolver before `requestType` queries; all `where` use resolved `organizationId`. |
| T-2-02 | Spoofing | Chat ingest secret unchanged; org from resolver only. |
</threat_model>

<task type="code">
<name>Slack slash: resolve org before catalog reads</name>
<files>src/app/api/slack/slash/route.ts</files>
<action>After signature verification, parse `team_id` and call `resolveOrgForSlackTeamId(teamId)`. On failure return `Response.json({ error, code }, { status })` consistent with `src/app/api/v1/requests/route.ts`. Remove any `db.select().from(organization).limit(1)` pattern. Ensure no `requestType` query runs before successful resolution.</action>
<verify>
<automated>npm run test:unit</automated>
`rg -n "resolveOrgForSlackTeamId" src/app/api/slack/slash/route.ts` matches.
`rg -n "limit\\(1\\)" src/app/api/slack/slash/route.ts` has no organization pick-first-org pattern (manual review if needed).
</verify>
<done>Unmapped Slack workspace gets explicit error before catalog access (TNTY-04).</done>
</task>

<task type="code">
<name>Chat ingest: use resolveChatIngestOrgId</name>
<files>src/app/api/v1/ingest/chat/route.ts</files>
<action>Replace direct `CHAT_INGEST_ORG_ID` usage with `resolveChatIngestOrgId()` after secret checks. Use returned `organizationId` for `findRequestTypeBySlug`, user lookup, and `createRequestCore`. Fail-closed JSON on resolver error.</action>
<verify>
<automated>npm run test:unit</automated>
`rg -n "resolveChatIngestOrgId" src/app/api/v1/ingest/chat/route.ts` matches.
</verify>
<done>Ingest path uses one verified org per successful request (TNTY-01, TNTY-02).</done>
</task>

<task type="code">
<name>Admin: Slack Team ID field for session org</name>
<files>src/app/actions/admin.ts;src/app/(dashboard)/admin/integrations/page.tsx;src/app/(dashboard)/admin/integrations/integrations-form.tsx</files>
<action>Extend admin org update action to persist optional `slackTeamId` (trim, max length, empty clears to null). Scope to session org only. Add form field with helper text. Handle unique constraint violation with clear error. Run `npm run lint` and `npm run build`.</action>
<verify>
<automated>npm run build</automated>
`rg -n "slackTeamId|slack_team_id" src/app/actions/admin.ts src/app/(dashboard)/admin/integrations/` matches.
</verify>
<done>Operators can map Slack workspace id without SQL (TNTY-04).</done>
</task>

## Verification

- `node ... gsd-tools.cjs verify plan-structure .planning/phases/01-canonical-tenant-resolution/01-PLAN-02.md` → valid
- `npm run test` green before phase verify

## Success criteria

- [ ] Slack + ingest + admin aligned with resolver-first ordering
- [ ] No regression on API v1 or dashboard session org patterns

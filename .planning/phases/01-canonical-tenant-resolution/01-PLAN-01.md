---
phase: 01-canonical-tenant-resolution
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/app-schema.ts
  - drizzle/
  - src/db/schema.ts
  - src/server/tenant-resolution.ts
  - tests/tenant-resolution.test.ts
  - .planning/phases/01-canonical-tenant-resolution/01-CHANNEL-TRACE.md
autonomous: true
requirements:
  - TNTY-01
  - TNTY-02
must_haves:
  truths:
    - "organization.slack_team_id is unique when set (nullable allowed)."
    - "resolveOrgForSlackTeamId and resolveChatIngestOrgId fail closed with explicit codes."
  artifacts:
    - path: src/server/tenant-resolution.ts
      contains: resolveOrgForSlackTeamId
    - path: tests/tenant-resolution.test.ts
      contains: tenant-resolution
  key_links:
    - from: src/server/agent-auth.ts
      to: src/server/tenant-resolution.ts
      pattern: organizationId
---

# Plan 01 — Schema, canonical resolver, tests

## Objective

Add persisted Slack workspace mapping on `organization`, implement `src/server/tenant-resolution.ts` with fail-closed semantics for Slack and chat-ingest org resolution, and lock behavior with unit tests after schema is applied.

## Context

- @.planning/phases/01-canonical-tenant-resolution/01-RESEARCH.md
- @src/db/app-schema.ts
- @src/server/agent-auth.ts
- @src/app/api/v1/requests/route.ts

<threat_model>
## Trust boundaries

| Boundary | Risk |
|----------|------|
| Slack team id → DB | Must not select org without unique mapping; no unscoped `limit(1)`. |

## STRIDE (summary)

| ID | Category | Mitigation |
|----|----------|------------|
| T-1-01 | Elevation | Unique index on `slack_team_id`; resolver rejects 0 or >1 rows. |
| T-1-02 | Information disclosure | Stable `code` in JSON; no raw tenant leakage in messages. |
</threat_model>

<task type="code">
<name>Add slack_team_id to organization (Drizzle + migration)</name>
<files>src/db/app-schema.ts;src/db/schema.ts;drizzle/</files>
<action>Add nullable `slackTeamId` (column `slack_team_id`) on `organization` with a unique index on non-null values. Generate SQL under `drizzle/` via `npx drizzle-kit generate` (or repo-equivalent). Export through existing schema barrel. Do not run app tests that need DB until Task 2 completes.</action>
<verify>
<automated>npm run build</automated>
`rg -n "slack_team_id|slackTeamId" src/db/app-schema.ts` returns matches.
A new `drizzle/*.sql` (or generated migration) contains slack_team_id and unique constraint/index.
</verify>
<done>Build passes; migration artifact exists with unique mapping for Slack team id.</done>
</task>

<task type="code">
<name>[BLOCKING] Apply schema to dev database</name>
<files>drizzle.config.ts</files>
<action>With dev `DATABASE_URL` set, run `npx drizzle-kit push` until exit code 0. Record success in phase SUMMARY. Do not edit migration SQL after this without re-running push.</action>
<verify>
<automated>npx drizzle-kit push</automated>
Command completes with exit code 0 against the configured database.
</verify>
<done>Dev DB schema includes `slack_team_id` and unique index.</done>
</task>

<task type="code">
<name>Implement tenant-resolution.ts and unit tests</name>
<files>src/server/tenant-resolution.ts;tests/tenant-resolution.test.ts</files>
<action>
Create `src/server/tenant-resolution.ts` exporting:
- `resolveOrgForSlackTeamId(teamId: string)` — discriminated union: success `{ ok: true, organizationId, name }` or failure `{ ok: false, httpStatus, code, message }`. Empty teamId → fail-closed. Query `organization` by `slackTeamId` equality only (never unscoped `limit(1)`).
- `resolveChatIngestOrgId()` — read `CHAT_INGEST_ORG_ID`, verify a row exists in `organization`; else fail-closed with explicit code (TNTY-02).

Add `tests/tenant-resolution.test.ts` (Vitest) covering fail-closed branches and success path with mocked DB or test harness per repo patterns.
Run `npm run test:unit`.
</action>
<verify>
<automated>npm run test:unit</automated>
`rg -n "resolveOrgForSlackTeamId|resolveChatIngestOrgId" src/server/tenant-resolution.ts` matches.
`rg "from\\(organization\\).*limit\\(1\\)" src/server/tenant-resolution.ts` returns no matches.
</verify>
<done>All new unit tests pass; resolver exports are used only through this module for Phase 1 follow-up wiring.</done>
</task>

<task type="code">
<name>Traceability: document unchanged channels for TNTY-01</name>
<files>.planning/phases/01-canonical-tenant-resolution/01-CHANNEL-TRACE.md</files>
<action>Create `01-CHANNEL-TRACE.md` in this phase directory with heading `## Channel coverage` and bullets: (1) Dashboard/UI: org from Better Auth `user.organizationId` / session — unchanged in Plan 01–02 except admin Slack field. (2) API v1: org from `resolveAgentApiKey` — unchanged. (3) Slack slash + chat ingest: changed in Plan 02. (4) Other webhooks: out of scope unless listed in Plan 02.</action>
<verify>
<automated>rg -n "Channel coverage" .planning/phases/01-canonical-tenant-resolution/01-CHANNEL-TRACE.md</automated>
File exists and contains `Channel coverage`.
</verify>
<done>TNTY-01 traceability explicit for UI/API vs Slack/chat work split.</done>
</task>

## Verification

- `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify plan-structure .planning/phases/01-canonical-tenant-resolution/01-PLAN-01.md` → valid
- `npm run test:unit` green

## Success criteria

- [ ] Schema + push + resolver + tests complete
- [ ] Plan 02 can import resolver for Slack/chat/admin wiring
